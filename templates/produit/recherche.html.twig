<div class="container" style="margin-top: -100px">
    {{  form_start(form) }}
    {{ form_row(form.q) }}
    <h4 style="color: #2aabd2">Catégories</h4>
    {{ form_row(form.categories) }}
    <h4 style="color: #2aabd2">Prix</h4>
    <div class="flex" style="padding: 30px">
        <div class="col-md-6">
            {{ form_row(form.min) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.max) }}
        </div>
    </div>

    <button id="test" type="submit" class="btn btn-primary w-100">Rechercher</button>
    {{ form_end(form) }}

<script>

    document.getElementById('test').addEventListener('click',window.history.pushState("object or string", "Title", "http://127.0.0.1:8000/front_prod"))
</script>


</div>