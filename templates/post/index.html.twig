{% extends 'base-front.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}
    <div class="breadcrumb-section breadcrumb-bg">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 offset-lg-2 text-center">
                    <div class="breadcrumb-text">
                        <p>Echangez vos pensées et vos idées</p>
                        <h1>Notre Blog</h1>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="latest-news mt-150 mb-150">
        <div class="container">
            <div class="row">
                {% for post in posts %}
                <div class="col-lg-12 col-md-6">
                    <div class="single-latest-news">
                        <a href="{{ path('post_show', {"id": post.id}) }}"><div class="latest-news-bg news-bg-2"></div></a>
                        <img src="{{ post.image }}" alt="{{ post.image }}">
                        <div class="news-text-box">
                            <h3><a href="{{ path('post_show', {"id": post.id}) }}">Blog title</a></h3>
                            <p class="blog-meta">
                                <span class="author"><i class="fas fa-user"></i> Admin</span>
                                <span class="author"><i class="fas fa-user"></i> Nombre de réactions:{{ post.nbrReaction }}</span>
                                <span class="author"><i class="fas fa-user"></i> Nombre de commentaires:{{ post.nbrCommentaire }}</span>
                            </p>
                            <p class="excerpt">{{ post.contenu }}</p>
                            <a href="{{ path('post_show', {"id": post.id}) }}" class="read-more-btn">read more <i class="fas fa-angle-right"></i></a>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
