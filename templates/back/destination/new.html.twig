{% extends 'base-back.html.twig' %}

{% block title %}New Destination{% endblock %}

{% block body %}
<div class="col-md-6 grid-margin stretch-card" style="margin-left: auto;margin-right: auto">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Ajouter une destination</h4>
            {{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}

            {{ include('back/destination/_form.html.twig') }}
            {{ form_rest(form) }}
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
