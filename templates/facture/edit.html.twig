{% extends 'base-back.html.twig' %}

{% block body %}
    <h1>Edit Facture</h1>

    {{ include('facture/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('facture_index') }}"class="btn btn-success">back to list</a>

    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}
