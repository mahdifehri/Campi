<!--PreLoader-->
<div class="loader">
    <div class="loader-inner">
        <div class="circle"></div>
    </div>
</div>
<!--PreLoader Ends-->

<!-- header -->
<div class="top-header-area" id="sticker">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 col-sm-12 text-center">
                <div class="main-menu-wrap">
                    <!-- logo -->
                    <div class="site-logo">
                        <a href="index.html">
                            <img src="{{ asset("front-office/img/logo.png") }}" alt="">

                        </a>
                    </div>
                    <!-- logo -->

                    <!-- menu start -->
                    <nav class="main-menu">
                        <ul>

                            <li><a href="{{ path('destination_front') }}">Destinations</a></li>

                            <li><a href="{{ path('evenement') }}">Evénements</a>
                                <ul class="sub-menu">
                                    <li><a href="{{ path('evenement') }}">Evénements</a></li>
                                    {% if is_granted('ROLE_USER') %}
                                        <li><a href="{{ path('mesevenements') }}">Mes évenements</a></li>
                                    {% endif %}
                                </ul>
                            </li>
                            <li><a href="{{ path('post_index') }}">Postes</a>
                                <ul class="sub-menu">
                                    <li><a href="{{ path('post_index') }}">Postes</a></li>
                                    {% if is_granted('ROLE_USER') %}
                                        <li><a href="{{ path('mes_postes') }}">Mes postes/commentaires</a></li>
                                    {% endif %}
                                </ul>
                            </li>
                            <li><a href="{{ path('contact') }}">Contactez-nous</a></li>
                            <li><a href="{{ path('front_prod') }}">Produits</a></li>
                            <li><a href="{{ path('reclamation_new') }}">Reclamation</a></li>

                            {% if is_granted('ROLE_USER') %}
                                <li><a href="{{ path('profile') }}">Profile:{{ app.user.prenom }} </a></li>
                            {% endif %}

                            {% if is_granted('ROLE_ADMIN') %}
                                <li><a href="{{ path('user_index') }}">Admin Room</a></li>
                            {% endif %}
                            {% if is_granted('ROLE_FOUR') %}
                                <li><a href="{{ path('produit') }}">Fournisseur Room</a></li>
                            {% endif %}
                            {% if is_granted('ROLE_USER') %}
                                <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
                            {% endif %}

                            {% if not is_granted('ROLE_USER') %}
                                <li><a  href="{{ path('app_register') }}">Inscription</a></li>
                                <li><a  href="{{ path('app_login') }}">Connexion</a></li>
                            {% endif %}
                            <li>
                                <div class="header-icons">
                                    <a class="shopping-cart" href="cart.html"><i class="fas fa-shopping-cart"></i></a>
                                    <a class="mobile-hide search-bar-icon" href="#"><i class="fas fa-search"></i></a>
                                </div>
                            </li>
                        </ul>
                    </nav>
                    <a class="mobile-show search-bar-icon" href="#"><i class="fas fa-search"></i></a>
                    <div class="mobile-menu"></div>
                    <!-- menu end -->
                </div>
            </div>
        </div>
    </div>
</div>
<!-- end header -->

<!-- search area -->
<div class="search-area">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <span class="close-btn"><i class="fas fa-window-close"></i></span>
                <div class="search-bar">
                    <div class="search-bar-tablecell">
                        <h3>Search For:</h3>
                        <input type="text" placeholder="Keywords">
                        <button type="submit">Search <i class="fas fa-search"></i></button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- end search area -->

